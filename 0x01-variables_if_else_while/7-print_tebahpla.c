#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)

{
char alphabet;

for (alphabet = 100 ; alphabet >= 99; alphabet--)
{
putchar(alphabet);
}

putchar('\n');

return (0);

}
